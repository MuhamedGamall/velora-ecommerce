/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.mrporter.com",
      "img.icons8.com",
      "lh3.googleusercontent.com",
      "cdn.sanity.io",
    ],
  },
};

export default nextConfig;
