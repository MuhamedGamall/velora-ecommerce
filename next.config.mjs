/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.mrporter.com",
      "img.icons8.com",
      "lh3.googleusercontent.com",
    ],
  },
};

export default nextConfig;
